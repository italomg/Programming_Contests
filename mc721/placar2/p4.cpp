#include<bits/stdc++.h>

using namespace std;

int main(){

  int N, K;

  while(cin>>N>>K){
    vector<int> fila(N), grupo_max(K), grupo_min(K);
    vector<pair<int, int> > max_min(N);

    fila[0] = 0;
    for(int i = 1; i < N; i++)
      scanf("%d", &fila[i]);

    for(int i = 0; i < N; i++){
      for(int j = 0; j < N; j++){
	
      }
    }
    
    
  }


  
  return 0;
}
